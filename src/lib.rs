mod document;
mod field;

use crate::document::Document;

// search engine struct
struct IndexWriter {}

impl IndexWriter {
    //add a document
    fn index(doc: &Document) {}
}

struct IndexReader {}
